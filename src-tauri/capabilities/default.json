{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Splice It main window. Native open/save dialogs are required for importing source audio and choosing an export destination. All file reading and writing is performed by the app's own Rust commands, so the fs plugin scope is intentionally not granted. Window close and destroy are needed so the unsaved-changes prompt can cancel or complete a quit.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "dialog:default"
  ]
}
